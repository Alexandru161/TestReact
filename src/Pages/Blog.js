import React from 'react';

export default function Blog() {
    return (
        <div style={{ padding: '100px 20px' }}>
            <h1>Blog</h1>
            <p>📝 Nothing here yet, but stay tuned for updates!</p>
        </div>
    );
}
